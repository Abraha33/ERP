# Copiar a apps/mobile/.env (Expo carga este directorio al ejecutar npm run start aquí).
# Valores típicos = mismos que en la raíz del repo (.env.example): URL y anon key públicas.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_API_BASE_URL=

# Pantalla dev (solo __DEV__): Tab One → «Dev: Supabase health / RLS» → /dev/supabase-health
