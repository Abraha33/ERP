**Milestone:** M0 · Workflow & Foundation  
**Talla:** M | **Horas:** 2 h | **Prioridad:** Alta

## Sub-issues

Desglose en issues enlazados (crear o asignar números al crear):

- **W07a** — Migración `001_profiles` en `supabase/migrations/` (convenciones W04)
- **W07b** — `supabase db push` local
- **W07c** — RLS mínima (admin/sesión válida puede leer; anon → 0 filas)

## Prueba de cierre global

`supabase db diff` sin pendientes. Query con `anon_key` a `public.profiles` → **0 rows** (RLS activa).

## Referencia en repo

`supabase/migrations/20260408130000_m0_001_profiles.sql` (revisar solapes).

`docs/milestones/M0-workflow-foundation.md`
