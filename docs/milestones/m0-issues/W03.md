**Milestone:** M0 · Workflow & Foundation  
**Talla:** XS | **Horas:** 0,5 h | **Prioridad:** Alta

## Tareas

- [ ] `.env.example` en raíz con las 14 variables del stack (o conjunto canónico ADR)
- [ ] `.gitignore` incluye `.env` y `.env.local`
- [ ] README apunta a `.env.example`

## Prueba de cierre

`.env.example` existe en `main`, `.env` no aparece en `git status`.

## Referencia

`docs/milestones/M0-workflow-foundation.md`
